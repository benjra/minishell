#include "mini.h"
void    program_name(char **en)
{
    char *prom;
    t_name *env;

    env=fill_env(en);
    int i=0;
    while(1)
    {

        prom=ft_strdup(GREENB);
        readline(prom);//u should know how to work with readline 
        
    }
}
